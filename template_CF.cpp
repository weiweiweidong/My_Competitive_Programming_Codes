#include <bits/stdc++.h>
using namespace std;
typedef long long LL;

LL n;
inline void solve() {
    cin >> n;
}

inline void pre_work() {}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    pre_work();

    int T = 1;
    cin >> T;
    while (T--)
        solve();
    return 0;
}